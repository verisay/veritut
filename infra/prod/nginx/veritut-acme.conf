# ACME HTTP-01 — 443 bloklarında da açık olmalı: Cloudflare "Always Use HTTPS" açıkken
# Let's Encrypt doğrulaması https'e yönlenir ve yalnız 80'de duran webroot'a hiç ulaşmaz.
location ^~ /.well-known/acme-challenge/ {
  root /var/www/acme;
  default_type "text/plain";
}
