# Mikro karar kaydı

Plan boşluğunda alınan kararlar: `tarih · karar · gerekçe · dosya`. Plan revizyonu gerektirenler burada DEĞİL, planda.

| # | Tarih | Karar | Gerekçe | Dosya |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-06 | Test koşucusu `node:test` + `tsx` (Vitest yerine) | Klasman/KD kalıbı; bağımlılık listesi kısa kalır. Vitest listede kalır, gerekirse geçilir | `*/package.json` `test` |
| 2 | 2026-09-06 | Runner ↔ API iletişimi: Redis (BullMQ) + `/internal/*` (INTERNAL_TOKEN); runner'da DB kimlik bilgisi yok | D5 patlama yarıçapı; Klasman worker deseni | `apps/runner/src/index.ts`, `routes/internal.routes.ts` |
| 3 | 2026-09-06 | OIDC code flow `jose` + fetch ile açık uygulama (`openid-client` kullanılmadı) | İç/dış Keycloak host ayrımı (hairpin) için endpoint'leri elle iç URL'e çevirmek gerekiyor; şeffaf ve 150 satır. `openid-client` izinli listede kalır | `apps/api/src/lib/oidc.ts` |
| 4 | 2026-09-06 | Oturumdaki Keycloak refresh/id token'ları simetrik AES-256-GCM (`SESSION_ENC_KEY`) ile; D12 asimetrik zarf yalnız tedarikçi/iş yükü sırları için | Oturum token'ını API'nin kendisi kullanır (logout/refresh), asimetrik zarf anlamsız | `apps/api/src/lib/crypto.ts` |
| 5 | 2026-09-06 | Dev realm'lerinde ops MFA `CONFIGURE_TOTP` zorunlu DEĞİL; prod realm export'unda zorunlu | Smoke/otomasyon için; D6 prod kuralı değişmedi | `infra/keycloak/realms/veritut-ops.json` |
| 6 | 2026-09-06 | Dev Redis ACL: `status` kullanıcısı sabit dev parolası ile (`status-dev-only`) | ACL dosyası env interpolasyonu yapmaz; prod'da EnvironmentFile'dan üretilir | `infra/redis/users.acl` |
| 7 | 2026-09-06 | Personel rolü Keycloak realm rolünden (`roles` claim, ID token mapper) — DB `staff.role` her girişte senkronlanır | Rol yönetimi tek yerde (Keycloak); break-glass hariç | `routes/auth.routes.ts` `highestStaffRole` |
| 8 | 2026-09-06 | Aktif kiracı seçimi portalda `vt_tenant` çerezi (HttpOnly değil, yetki taşımaz); yetki her istekte `X-Tenant-Id` + üyelikle doğrulanır | Çerez yalnız UI tercihi; güvenlik `resolveTenant`'ta | `apps/portal/src/routes/panel/+layout.server.ts` |
| 9 | 2026-09-06 | `evidence_events.tenant_id NULL` = platform zinciri; kısmi UNIQUE index (`seq WHERE tenant_id IS NULL`) | Tek tablo, iki zincir sınıfı; ayrı tablo şişkinliği yok | `0001_init.sql` |
| 10 | 2026-09-06 | Portal `(public)` + `/panel` aynı SvelteKit app'te route grubu (base path yok) | KD `docs/prod.md` base-path tuzağı; çerez tek host | `apps/portal/src/routes/` |
| 11 | 2026-09-06 | Runner imajında OpenTofu/Ansible/Restic `INSTALL_IAC` build arg'ı ile (varsayılan 1); ağ kısıtlı ortamda 0 | K0 yalnız echo koşar; imaj yine de D10 araçlarını taşımalı | `infra/Dockerfile.runner` |
| 12 | 2026-09-06 | İç servis URL'leri proje-özel DNS alias ile (`veritut-api`, `veritut-keycloak`, `veritut-portal`, `veritut-ops`) | Traefik ağı filoda paylaşımlı; çıplak `api` adı CarRentall API'sine çözüldü (ops 500: "X-CRL-Surface başlığı gerekli"). Filo geneli tuzak | `infra/compose.dev.yml`, `infra/.env.example`, `db/seed.ts` |
| 13 | 2026-09-06 | Runner `backup` kind: K1'de restic runner konteynerinde koşar (hedef VM'de değil); yol yoksa iş yükü meta verisinden örnek küme alınır | Yedek→kanıt boru hattını gerçek restic + S3 ile K1'de kanıtlamak; hedef sunucu yolları Ansible ile K2'de | `apps/runner/src/jobs/backup.ts` |
| 14 | 2026-09-06 | Gelir K1'de elle (`workload_revenue`), K3'te faturalama aynası devralır; marj TRY tabanında `fx_rates` ile karşılaştırılır | Stratejik 'İlk 30 gün' madde 3 (gerçek brüt marj) faturalama omurgası beklemeden cevaplanmalı | `services/cost.service.ts` |
| 15 | 2026-09-06 | Fatura CSV satırı envanter external_id ile eşleşirse `direct` tahsis yazılır ve aynı dönemin `estimate` tahsisi silinir | Gerçek maliyet tahmini ezer; iki kez sayılmaz | `cost.service.ts importInvoiceCsv` |
| 16 | 2026-09-06 | Erişim şeffaflığı: `access.session` kanıtı + kiracıya bildirim ingest anında (onay/gizleme yok) | Plan §6.5 — satış argümanı; personel erişimi müşteriden gizlenmez | `services/access.service.ts` |
| 17 | 2026-09-06 | Blueprint `inputs` = JSON Schema alt kümesi (string/number/integer/boolean, enum, min/max, pattern, format slug/hostname/email/url, secret, ops_only); Zod'a derlenir, ops/portal formu bundan üretilir | Tek doğrulama kaynağı (plan §3.4); tam JSON Schema motoru bağımlılık listesine girmez | `packages/validators/src/blueprint-compiler.ts` |
| 18 | 2026-09-06 | OpenTofu state: pg backend, iş yükü başına şema `w_<uuid>`, `TF_ENCRYPTION` (pbkdf2 + aes_gcm, enforced) runner env'inden | D11; passphrase yalnız runner'da, API/DB'de yok | `apps/runner/src/jobs/provision.ts tfEnv` |
| 19 | 2026-09-06 | İş yükü kilidi Redis `SET NX` (runner'da DB yok); aynı runId yeniden alabilir (çökme sonrası devam) | Plan advisory lock'u pg'de öngörüyordu; runner'ın DB kimliği olmadığı için Redis | `provision.ts acquireLock` |
| 20 | 2026-09-06 | Erişim bilgisi (`access` çıktısı) runner'dan API'ye iç kanalda düz gelir, API `RUNNER_PUBLIC_KEY` ile mühürler ve bir daha okuyamaz | D12'yi korur; runner'a ayrıca mühürleme kodu koymaz | `provisioning.service.ts registerWorkload` |
| 21 | 2026-09-06 | Uygulama blueprint'lerinde Keycloak OIDC istemcisi `veritut-musteri` realm'inde `<tenant>-<workload>` clientId ile açılır (kiracı başına realm ERTELENDİ) | Müşteri VERITUT hesabıyla girer (asıl vaat); realm-per-tenant K5'te (offboarding kolaylığı) | `_roles/keycloak-oidc-client` |
| 22 | 2026-09-06 | Reddetme dört-göze tabi değil (talep eden geri çekebilir); onay tabi. Ret resize/upgrade'de eski boyut+active, destroy'da active'e döner | Geri çekme güvenli yön; onay riskli yön | `provisioning.service.ts rejectRun` |
| 23 | 2026-09-06 | mock-vps blueprint'i `terraform_data` (provider indirmesiz) + local Ansible; `server_type` → `triggers_replace` ile boyut değişimi replace üretir | Onay akışı ve devamlılık gerçek tofu ile ama bulut hesabı olmadan test edilir | `infra/blueprints/mock-vps` |
| 24 | 2026-09-06 | Katalog iki katmanlı: `products` (satış) + `product_versions` (blueprint sürümü). Üründe TEK `published` sürüm (kısmi unique index) | Self-servis alıcı sürüm seçmez; ops yayımlar, müşteri hep güncel sürümü alır | `0004`, `catalog.service.ts` |
| 25 | 2026-09-06 | Fiyat = ürün × boyut × ikametgâh × plan × SLA × para birimi, `valid_from` ile sürümlü; sipariş anındaki tutar `orders` satırına kopyalanır | Fiyat değişimi geçmiş sözleşmeyi etkilemez ("sipariş anındaki fiyat sözleşmedir") | `price_list`, `orders.monthly` |
| 26 | 2026-09-06 | Abonelik iş yükü başına (`tenant_subscriptions.workload_id` kısmi unique); kiracının etkin planı = aktif aboneliklerin en yüksek sıralı planı | Kiracı farklı iş yüklerinde farklı plan alabilir; entitlement en geniş plana göre | `entitlement.service.ts effectivePlanCode` |
| 27 | 2026-09-06 | Deneme: kiracı başına bir kez, en küçük boyut, kart yok; bitişte abonelik `suspended` + iş yükü durur, veri silinmez, 7 gün sonra yıkım adayı | Stratejik §9 risk 4 (ücretsiz katman sızması) kodla kapatıldı | `order.service.ts`, `billing.service.ts expireTrials` |
| 28 | 2026-09-06 | Public API prefix `/api/v1/ext` (api.veritut.com aynı uygulamaya düşer); kiracı bağlamı ANAHTARDAN gelir, `X-Tenant-Id` kabul edilmez | Anahtar sızsa bile başka kiracıya geçilemez | `ext.routes.ts`, `apiKeyAuth.ts` |
| 29 | 2026-09-06 | Ürün landing'lerinde markdown için `marked` yerine 30 satırlık alt küme renderer'ı | Public sayfada JS bütçesi 0 hedefi (D21) ve kapalı bağımlılık listesi (D26) | `urunler/[slug]/+page.svelte` |
| 30 | 2026-09-06 | Mock faturalama/destek sağlayıcıları durumu Redis'te tutar ve gerçek sağlayıcıyla AYNI webhook yolunu tetikler | Kod yolu dev ile prod arasında ayrışmaz; FOSSBilling/Zammad'a geçiş adaptör değişimidir | `providers/billing/mock.ts`, `providers/ticket/mock.ts` |
| 31 | 2026-09-06 | Alarm kanalı hedefi simetrik şifreli (`SESSION_ENC_KEY`), D12 asimetrik zarfı DEĞİL | Gönderimi API/worker yapar; runner'ın açması gereken bir sır değil | `services/channel.service.ts` |
| 32 | 2026-09-06 | Kanal ipucu maskesi: e-posta `ab***@alan`, SMS son 4 hane, webhook `şema://ana-makine/***` — yol gizlenir, ana makine gösterilir | Sır webhook YOLUNDA taşınır (Slack/Teams token'ı); ana makine müşterinin kendi bilgisidir ve kanalı ayırt etmeye yarar | `channel.service.ts hint()` |
| 33 | 2026-09-06 | SLA çözüm hedefi = katmanın `resolveMin` × ciddiyet çarpanı (sev1 0.5 · sev2 1 · sev3 2 · sev4 4); yanıt hedefi ciddiyetten bağımsız | Aynı SLA katmanında sev1 ile sev4 aynı çözüm süresine bağlanamaz; yanıt süresi ise her olayda aynı taahhüttür | `packages/types/src/guvence.ts` |
| 34 | 2026-09-06 | Olayın SLA katmanı, etkilenen iş yüklerinin EN SIKI katmanıdır (en kısa yanıt süresi) | Karma etkide müşteri lehine yorum | `incident.service.ts resolveSla` |
| 35 | 2026-09-06 | Alarmdan açılan olay varsayılan olarak müşteriye KAPALI; görünürlüğü operatör açar | Yanlış alarmla müşteriyi telaşlandırmamak; şeffaflık gerçek olayda başlar | `alert.service.ts` |
| 36 | 2026-09-06 | `pnpm check` turbo eşzamanlılığı 2 ile sınırlı | AWS SDK + Svelte tipleri paralel derlenince 15 GB dev makinede tsc SIGTERM alıyor (ölçüldü) | kök `package.json` |

| 37 | 2026-09-07 | Prod TEK makinede (`2.29.31.37` · `veritut-prod`): app + veri + kimlik + runner + status + MinIO aynı kutuda; izolasyon UNIX kullanıcısı + systemd ile (`veritut`, `veritut-runner`, `veritut-status`, `keycloak`, `minio`) | Plan §13 altı sunucu ister; bütçe/karmaşıklık için tek sunucuda başlanıyor. D5/D12/D14 **uygulama** sınırları korunuyor, **fiziksel** ayrım korunmuyor — B1 altında açık borç | `infra/prod/` |
| 38 | 2026-09-07 | Sır dağıtımı `/etc/veritut/secrets.env` (0600 root) tek kaynak → servis başına env dosyası; `runner.env` grup `veritut-runner` (uygulama kullanıcısı okuyamaz), `status.env` yalnız `STATUS_REDIS_URL` | D12/D14'ün dev'deki compose ezmesinin prod karşılığı; smoke her deploy'da okunabilirliği ölçer | `infra/prod/render-env.sh`, `smoke.sh` |
| 39 | 2026-09-07 | Keycloak yönetim arayüzü portu 9990 (varsayılan 9000 değil) | MinIO 9000'de; Keycloak yönetim arayüzü de 9000'e bağlanmaya çalışıp "Address already in use" ile düşüyor (import komutunda da) | `render-env.sh` `KC_HTTP_MANAGEMENT_PORT` |
| 40 | 2026-09-07 | Seed'in platform bileşeni probe adresleri env'den (`API_URL`, `PORTAL_INTERNAL_URL`, `OPS_INTERNAL_URL`, `KC_INTERNAL_URL`) | Adresler dev compose alias'larına gömülüydü; prod'da status sayfası tüm bileşenleri "down" gösterirdi | `apps/api/src/db/seed.ts` |
| 41 | 2026-09-07 | Prod realm'leri dev JSON'larından üretilir (`render-realms.mjs`): prod host'ları, üretilmiş istemci sırları, dev kullanıcıları yok, ops realm'inde `CONFIGURE_TOTP` varsayılan zorunlu, `sslRequired=all` | Realm tanımının tek kaynağı dev JSON'u kalsın, prod farkı kodda görünsün (D6, B8) | `infra/prod/render-realms.mjs` |
